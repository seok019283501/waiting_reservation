spring:
  application:
    name: waiting_reservation_be
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reservation?useUnicode=true&allowPublicKeyRetrieval=true
    username: 
    password:
  thymeleaf:
    cache: false